/**
 * 绑定methods内部this指向,并且绑定到target
 * @param isCustomElement
 * @param target
 * @param root
 * @param methods
 * @param targetProxy
 */
export default function initMethods(target: any, methods: any, targetProxy: any): void;
